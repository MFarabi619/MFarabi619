description: |
  DFRobot SEN0601 soil moisture + temperature + EC + salinity + TDS sensor
  (Modbus RTU, 5 holding registers).

compatible: "dfrobot,soil-moisture-three-in-one"

include: [sensor-device.yaml]

properties:
  slave-id:
    type: int
    required: true
    description: Modbus slave address of this probe.
