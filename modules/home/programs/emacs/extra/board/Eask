;; -*- mode: eask; lexical-binding: t -*-

(package "board"
         "0.0.1"
         "Board pinout diagrams")

(website-url "https://github.com/MFarabi619/MFarabi619/modules/home/programs/emacs/extra/board")
(keywords "tools" "embedded" "hardware")

(package-file "board.el")

(depends-on "emacs" "29.1")

(script "check" "eask lint checkdoc && eask lint declare && eask compile && eask clean elc && eask test activate && eask test buttercup")

(development
 (depends-on "buttercup"))
