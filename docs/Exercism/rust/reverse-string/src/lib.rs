pub fn withLoop(input &str) -> String {
  let mut output = "";
  loop
}

pub fn reverse(input: &str) -> String {
  // naive:
  // 1. create a new string, and assign characters from input with a for loop
    todo!("Write a function to reverse {input}");
}
