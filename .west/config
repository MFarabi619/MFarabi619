[manifest]
path     = .
file     = firmware/west.yml
project-filter = +zephyr-lang-rust

[zephyr]
base     = firmware/zephyr

[build]
pristine = always
runner   = probe-rs
# board    = walter/esp32s3/procpu
board    = esp32s3_devkitc/esp32s3/procpu

[flash]
rebuild = true

[alias]
# mylist = list -f '{name} {revision}'
# run = build -p always; flash; espressif monitor
