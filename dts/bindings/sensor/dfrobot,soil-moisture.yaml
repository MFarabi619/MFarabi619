description: |
  DFRobot SEN0600 soil moisture + temperature sensor (Modbus RTU, 2 holding registers).

compatible: "dfrobot,soil-moisture"

include: [sensor-device.yaml]

properties:
  slave-id:
    type: int
    required: true
    description: Modbus slave address of this probe.
