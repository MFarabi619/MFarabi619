description: DFRobot rainfall sensor (Modbus RTU)

compatible: "dfrobot,rainfall"

include: [sensor-device.yaml]

properties:
  slave-id:
    type: int
    required: true
    description: Modbus slave address of the rainfall sensor
