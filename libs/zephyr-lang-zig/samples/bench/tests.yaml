common:
  tags:
    - zephyr_lang_zig
  harness: console
  harness_config:
    type: one_line
    regex:
      - "=== bench done ==="

tests:
  zephyr_lang_zig.sample.bench:
    platform_allow:
      - qemu_riscv32
    integration_platforms:
      - qemu_riscv32
