common:
  tags:
    - zephyr_lang_zig
  harness: console
  harness_config:
    type: one_line
    regex:
      - "Hello World! \\w+"

tests:
  zephyr_lang_zig.sample.hello_world:
    platform_allow:
      - qemu_riscv32
    integration_platforms:
      - qemu_riscv32
