common:
  tags:
    - zephyr_lang_zig
  harness: console
  harness_config:
    type: one_line
    regex:
      - "sqlite sample done"

tests:
  zephyr_lang_zig.sample.sqlite:
    platform_allow:
      - qemu_riscv32
    integration_platforms:
      - qemu_riscv32
