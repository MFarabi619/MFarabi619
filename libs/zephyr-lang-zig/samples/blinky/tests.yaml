common:
  tags:
    - zephyr_lang_zig
  harness: console
  harness_config:
    type: multi_line
    ordered: true
    regex:
      - "blinky on led0"
      - "toggle"

tests:
  zephyr_lang_zig.sample.blinky:
    platform_allow:
      - qemu_riscv32
    integration_platforms:
      - qemu_riscv32
