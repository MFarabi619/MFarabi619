common:
  tags:
    - zephyr_lib_sqlite
  harness: console
  harness_config:
    type: multi_line
    ordered: true
    regex:
      - "SQLite [0-9.]+ on "
      - "sensor=temp"
      - "done"

tests:
  zephyr_lib_sqlite.sample.basic:
    platform_allow:
      - qemu_riscv32
    integration_platforms:
      - qemu_riscv32
