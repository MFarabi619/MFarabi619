CONFIG_ESPTOOLPY_HEADER_FLASHSIZE_UPDATE=y
CONFIG_ESPTOOLPY_FLASHFREQ_40M=y

# Board defconfigs (walter) assign this for every image; the bootloader
# strips serial interrupt support, so re-assign n to match its deps.
CONFIG_UART_INTERRUPT_DRIVEN=n
