CONFIG_NET_HOSTNAME="xiao"

CONFIG_ESPTOOLPY_OCT_FLASH=n

CONFIG_PWM=y
CONFIG_PWM_SHELL=y
CONFIG_PWM_LED_ESP32=y
CONFIG_GPIO_SHELL=y

CONFIG_SPI=y
CONFIG_LED_STRIP=y
CONFIG_WS2812_STRIP_SPI=y
CONFIG_LED_STRIP_SHELL=y
CONFIG_LED=y
CONFIG_LED_SHELL=y

CONFIG_HEAP_MEM_POOL_SIZE=32768

# NOTE: Adeept HAT (PCA9685 servos) — uncomment with the overlay's I2C block:
# CONFIG_I2C=y
# CONFIG_I2C_SHELL=y
# CONFIG_PWM_PCA9685=y

# NOTE: zenoh-pico shelved. Everything below was added for zenoh; the NET/POSIX
# lines are its prereqs and CONFIG_HEAP_MEM_POOL_SIZE=32768 carved 32 KB out of
# internal SRAM — the pool the ESP32-S3 WiFi driver needs — which regressed WiFi
# connects. Uncomment the whole block to resume zenoh. Open lead on the -78 OOM:
# the heap_caps route in zenoh-pico.patch uses MALLOC_CAP_8BIT — try
# MALLOC_CAP_SPIRAM. Driving via mcumgrctl for now.
# CONFIG_ZENOH_PICO=y
# CONFIG_ZENOH_PICO_SUBSCRIPTION=y
# CONFIG_ZENOH_PICO_MULTI_THREAD=y
# CONFIG_ZENOH_PICO_LINK_TCP=y
# CONFIG_ZENOH_PICO_UNICAST_TRANSPORT=y
# CONFIG_ZENOH_PICO_SESSION_CHECK=y
# CONFIG_ZENOH_PICO_AUTO_RECONNECT=y
# CONFIG_ZENOH_PICO_TCP_NODELAY=y
# CONFIG_NET_TCP=y
# CONFIG_NET_SOCKETS=y
# CONFIG_POSIX_API=y
# CONFIG_MAX_PTHREAD_MUTEX_COUNT=32
# CONFIG_MAX_PTHREAD_COND_COUNT=16
# CONFIG_HEAP_MEM_POOL_SIZE=32768
