diff --git a/boards/seeed/xiao_esp32s3/board.cmake b/boards/seeed/xiao_esp32s3/board.cmake
index 2f04d1fe886..6fea660caf5 100644
--- a/boards/seeed/xiao_esp32s3/board.cmake
+++ b/boards/seeed/xiao_esp32s3/board.cmake
@@ -7,3 +7,4 @@ find_program(OPENOCD openocd PATHS ${ESPRESSIF_TOOLCHAIN_PATH}/openocd-esp32/bin
 
 include(${ZEPHYR_BASE}/boards/common/esp32.board.cmake)
 include(${ZEPHYR_BASE}/boards/common/openocd.board.cmake)
+include(${ZEPHYR_BASE}/boards/common/probe-rs.board.cmake)
diff --git a/scripts/west_commands/runners/probe_rs.py b/scripts/west_commands/runners/probe_rs.py
index 909f1e28454..7ccfbb18067 100644
--- a/scripts/west_commands/runners/probe_rs.py
+++ b/scripts/west_commands/runners/probe_rs.py
@@ -22,6 +22,7 @@ class ProbeRsBinaryRunner(ZephyrBinaryRunner):
                  speed=None,
                  connect_under_reset=False,
                  verify=False,
+                 base_address=None,
                  tool_opt=None):
         super().__init__(cfg)
 
@@ -32,6 +33,7 @@ class ProbeRsBinaryRunner(ZephyrBinaryRunner):
         self.speed = speed
         self.connect_under_reset = connect_under_reset
         self.verify = verify
+        self.base_address = base_address
 
         self.args = ['--chip', chip]
 
@@ -89,6 +91,9 @@ class ProbeRsBinaryRunner(ZephyrBinaryRunner):
                             help='connect under reset')
         parser.add_argument('--verify', action='store_true',
                             help='verify flash after programming')
+        parser.add_argument('--base-address', type=lambda x: int(x, 0), default=None,
+                            help='base address for bin file flashing (e.g. 0x20000); '
+                                 'only applies when --file-type=bin')
         parser.add_argument('--gdb-host', default=DEFAULT_PROBE_RS_GDB_HOST,
                             help=f'probe-rs gdb host, defaults to {DEFAULT_PROBE_RS_GDB_HOST}')
         parser.add_argument('--gdb-port', type=int, default=DEFAULT_PROBE_RS_GDB_PORT,
@@ -114,6 +119,7 @@ class ProbeRsBinaryRunner(ZephyrBinaryRunner):
                                    speed=args.speed,
                                    connect_under_reset=args.connect_under_reset,
                                    verify=args.verify,
+                                   base_address=args.base_address,
                                    gdb_host=args.gdb_host,
                                    gdb_port=args.gdb_port,
                                    tool_opt=args.tool_opt)
@@ -156,6 +162,8 @@ class ProbeRsBinaryRunner(ZephyrBinaryRunner):
             flash_file = self.hex_name
             flash_format = 'hex'
 
+        if flash_format == 'bin' and self.base_address is not None:
+            download_args += [f'--base-address={self.base_address:#x}']
         download_args += ['--binary-format', flash_format, flash_file]
 
         self.check_call([self.probe_rs, 'download']
diff --git a/soc/espressif/common/CMakeLists.txt b/soc/espressif/common/CMakeLists.txt
index d8508e663cb..5cd535e17f6 100644
--- a/soc/espressif/common/CMakeLists.txt
+++ b/soc/espressif/common/CMakeLists.txt
@@ -106,8 +106,18 @@ board_runner_args(esp32 "--esp-flash-size=${esptoolpy_flashsize}MB")
 board_runner_args(esp32 "--esp-flash-freq=${CONFIG_ESPTOOLPY_FLASHFREQ}")
 board_runner_args(esp32 "--esp-flash-mode=${CONFIG_ESPTOOLPY_FLASHMODE}")
 board_runner_args(esp32 "--esp-idf-path=${ESP_IDF_PATH}")
+board_runner_args(esp32 "--esp-skip-flashed")
+board_runner_args(esp32 "--esp-diff")
 board_finalize_runner_args(esp32 "--esp-monitor-baud=${monitor_baud}")
 
+board_runner_args(probe-rs
+  "--chip" "${CONFIG_SOC}"
+  "--protocol" "jtag"
+  "--file-type" "bin"
+  "--base-address=${image_off}"
+)
+board_finalize_runner_args(probe-rs)
+
 message(STATUS "Image partition address: ${image_off}")
 
 # Look for cross references between bootloader sections
