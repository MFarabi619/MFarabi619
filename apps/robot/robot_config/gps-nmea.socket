[Unit]
Description=Raw NMEA (from gpsd) over TCP

[Socket]
ListenStream=0.0.0.0:9001
Accept=yes

[Install]
WantedBy=sockets.target

# sudo cp gps-nmea.socket gps-nmea@.service /etc/systemd/system/
# sudo systemctl daemon-reload
# sudo systemctl enable --now gps-nmea.socket
