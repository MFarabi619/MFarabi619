diff --git a/subsys/fs/shell.c b/subsys/fs/shell.c
index a7741f673a3..4c4e89f29b6 100644
--- a/subsys/fs/shell.c
+++ b/subsys/fs/shell.c
@@ -93,6 +93,8 @@ static struct fs_mount_t littlefs_mnt = {
 /* Maintenance guarantees this begins with '/' and is NUL-terminated. */
 static char cwd[MAX_PATH_LEN] = "/";
 
+void fs_shell_on_cwd_changed(void);
+
 static void create_abs_path(const char *name, char *path, size_t len)
 {
 	if (name[0] == '/') {
@@ -131,6 +133,7 @@ static int cmd_cd(const struct shell *sh, size_t argc, char **argv)
 
 	if (argc < 2) {
 		strcpy(cwd, "/");
+		fs_shell_on_cwd_changed();
 		return 0;
 	}
 
@@ -144,6 +147,7 @@ static int cmd_cd(const struct shell *sh, size_t argc, char **argv)
 		}
 
 		/* No need to test that a parent exists */
+		fs_shell_on_cwd_changed();
 		return 0;
 	}
 
@@ -163,6 +167,7 @@ static int cmd_cd(const struct shell *sh, size_t argc, char **argv)
 	strncpy(cwd, path, sizeof(cwd));
 	cwd[sizeof(cwd) - 1] = '\0';
 
+	fs_shell_on_cwd_changed();
 	return 0;
 }
 
@@ -922,6 +927,15 @@ SHELL_STATIC_SUBCMD_SET_CREATE(sub_fs_mount,
 );
 #endif
 
+const char *fs_shell_get_cwd(void)
+{
+	return cwd;
+}
+
+__weak void fs_shell_on_cwd_changed(void)
+{
+}
+
 SHELL_STATIC_SUBCMD_SET_CREATE(sub_fs,
 	SHELL_CMD(cd, NULL, SHELL_HELP("Change working directory", "[<path>]"), cmd_cd),
 	SHELL_CMD(ls, NULL, SHELL_HELP("List files in current directory", "[<path>]"), cmd_ls),
diff --git a/subsys/shell/Kconfig b/subsys/shell/Kconfig
index 2478bd87eee..152dfdfa6fe 100644
--- a/subsys/shell/Kconfig
+++ b/subsys/shell/Kconfig
@@ -79,7 +79,7 @@ config SHELL_PROMPT_CHANGE
 config SHELL_PROMPT_BUFF_SIZE
 	int "Shell prompt buffer size"
 	depends on SHELL_PROMPT_CHANGE
-	range 2 40
+	range 2 4096
 	default 10 if SHELL_MINIMAL
 	default 20
 	help
diff --git a/subsys/shell/shell.c b/subsys/shell/shell.c
index d7a08c9a5ab..1be288aa5e1 100644
--- a/subsys/shell/shell.c
+++ b/subsys/shell/shell.c
@@ -2107,20 +2107,21 @@ int shell_prompt_change(const struct shell *sh, const char *prompt)
 		return -EINVAL;
 	}
 
-	size_t prompt_length = z_shell_strlen(prompt);
+	size_t prompt_bytes = strlen(prompt);
+	size_t prompt_visible = z_shell_strlen(prompt);
 
 	if (!z_shell_trylock(sh, SHELL_TX_MTX_TIMEOUT)) {
 		return -EBUSY;
 	}
 
-	if (prompt_length + 1 > CONFIG_SHELL_PROMPT_BUFF_SIZE) {
+	if (prompt_bytes + 1 > CONFIG_SHELL_PROMPT_BUFF_SIZE) {
 		z_shell_unlock(sh);
 		return -EINVAL;
 	}
 
 	strcpy(sh->ctx->prompt, prompt);
 
-	sh->ctx->vt100_ctx.cons.name_len = prompt_length;
+	sh->ctx->vt100_ctx.cons.name_len = prompt_visible;
 
 	z_shell_unlock(sh);
 
diff --git a/subsys/shell/shell_utils.h b/subsys/shell/shell_utils.h
index c0e27146c46..4b744202f60 100644
--- a/subsys/shell/shell_utils.h
+++ b/subsys/shell/shell_utils.h
@@ -33,7 +33,40 @@ void z_shell_multiline_data_calc(struct shell_multiline_cons *cons,
 
 static inline uint16_t z_shell_strlen(const char *str)
 {
-	return str == NULL ? 0U : (uint16_t)strlen(str);
+	if (str == NULL) {
+		return 0U;
+	}
+
+	uint16_t visible = 0U;
+	const unsigned char *p = (const unsigned char *)str;
+
+	while (*p != '\0') {
+		if (*p == 0x1B && *(p + 1) == '[') {
+			p += 2;
+			while (*p != '\0' && (*p < 0x40 || *p > 0x7E)) {
+				p++;
+			}
+			if (*p != '\0') {
+				p++;
+			}
+			continue;
+		}
+		if (*p == '\n') {
+			visible = 0;
+			p++;
+			continue;
+		}
+		if (*p == '\r') {
+			p++;
+			continue;
+		}
+		if ((*p & 0xC0) != 0x80) {
+			visible++;
+		}
+		p++;
+	}
+
+	return visible;
 }
 
 char z_shell_make_argv(size_t *argc, const char **argv,
